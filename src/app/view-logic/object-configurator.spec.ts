import { ObjectConfigurator } from './object-configurator';

describe('ObjectConfigurator', () => {
  it('should create an instance', () => {
    expect(new ObjectConfigurator()).toBeTruthy();
  });
});
